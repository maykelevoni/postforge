// This is a placeholder schema - it will be replaced in Task 002
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Placeholder model - will be replaced in Task 002
model Placeholder {
  id Int @id @default(autoincrement())
}
